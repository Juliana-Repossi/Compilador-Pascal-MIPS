program ex2;
var
  // w, t: real;
  // x: real;
  // y: string;
  // z: boolean;
  // x1 : integer;
  // x2 : real;
  // x3 : boolean;
  // x4 : string;
  // x5, x6 : integer;
  // x7, x8 : real;
  // x9, x10 : boolean;
  // x11, x12 : string;
  x13 : real;
  x14 : string;
  


// const
//   PI = 3.14;
//   const_int = 3;
//   const_bool = true;
//   const_str = 'constante';

begin
  
  // t := 586.45;
  // w := 15.4 * t + t * t;
  // x := 2 * w - t;
  // x := x + 1;
  // x := (x + w) * 2;
  // x := (w - t) * (8 + 6) - 750 / 30.5; 
  // y := 'anything' + 'written';
  // z := true and false or true and not false;
  // z := (5 > 2) and (5 = 5) and (3 + 6 <> 89 * 1) or not (5 <= 10);
  // z := 5 > 2 and 5 = 5 and 3 + 6 <> 89 * 1 or not 5 <= 10;
  // x1 := 2;
  // x2 := 2.0;
  // x3 := false;
  // x4 := 'elaine e filipe e juliana';
  // x5 := 6;
  // x6 := 6;
  // x7 := 7.1;
  // x8 := 8.2;
  // x9 := false;
  // x10 := true;
  // x11 := 'elaine';
  // x12 := 'filipe';
  // x13 := 2 + 2 * 4;
  x13 := 8.2 - 4.3;
  // x14 := 'elaine' + 'filipe';
end.

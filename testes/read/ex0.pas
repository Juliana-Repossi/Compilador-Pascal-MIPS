program ex0;
var
    x : integer;
    y : real;
    z : boolean;
    w : string;
    arr_x : array[0..5] of integer;
    arr_y : array[0..5] of real;
    arr_z : array[0..5] of boolean;
    //arr_w : array[0..5] of string;  //nao provemos array of string

    i : integer;

begin
    i := 0;

    read(x);
    // write(x);
    // writeln(x);
    // writeln(2 + 5 + 3 + 10 - 5.0 - 8.5 * 50);
    read(y);
    // write(y);
    // writeln(y);
    writeln(' ');
    //read(z);  //nao eh pra ler, pois nao eh aceito na leitura
    // write(z);
    // writeln(z);
    writeln(' ');
    read(w);
    // write(w);
    // writeln(w);
    writeln(' ');
    
    // while i <= 6 do begin
        // read(arr_z[i]);
        // write(arr_z[i]);

    // end; //end do while




end.
